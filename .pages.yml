media:
  input: src/assets
  output: assets
content:
  - name: posts
    label: Posts
    path: src/content/blog
    type: collection
    fields:
      - { name: title, label: Title, type: string }
      - { name: description, label: Description, type: string }
      - name: pubDate
        label: PubDate
        type: date
        options:
          format: MM-dd-yyyy
      - { name: heroImage, label: HeroImage, type: image }
      
  